# Caddy reverse proxy for the Fortune U Group automation stack.
# Uses env placeholders defined in docker-compose (N8N_DOMAIN, CRM_DOMAIN).

{$N8N_DOMAIN} {
    reverse_proxy n8n:5678
}

{$CRM_DOMAIN} {
    reverse_proxy nocodb:8080
}
